{
  "name": "skill-hub-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix fronted run dev",
    "build": "npm --prefix fronted run build",
    "tauri": "npm --prefix fronted run tauri"
  }
}
